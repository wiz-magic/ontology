# STAGING-LOG

<!-- Audit trail of generator activity. append-only. Format: SCHEMA.md §9.3.
     Written by generators (or role agents whose write-scope includes staging).
     The canonical ontology agent reads this file but never appends to it —
     its candidate status receipts are logged in LOG.md under [INGEST].
     Dream cycles touch staging only and never modify the canonical store. -->
